#include <stdio.h>
#include <string.h>

int main(){

    char string1[] = "Hello";
    char string2[] = "World";

    //strlwr(string1);
    //strupr(string1);
    //strcat(string1, string2);
    //strncat(string1, string2, 3);
    //strcpy(string1, string2);
    //strncpy(string1, string2, 3);

    //strset(string1, '?');
    //strnset(string1, 'x', 3);
    //strrev(string1);

    int result;

    //result = strlen(string1);
    //result = strcmp(string1, string2); result 0 if same
    //result = strncmp(string1, string2, 3) first 3 char
    //result = strcmpi(string1, string 2) ignore case

    printf("%s\n", string1);
    printf("%d", result);
}